import React, { useState } from "react";
import "../App.css";

const Home: React.FC = () => {
  interface FormData {
    name: string;
    age: number;
  }
  const initialState: FormData = {
    name: "",
    age: 0,
  };

  return (
    <>

    </>
  );
};

export default Home;
